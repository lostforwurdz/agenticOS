---
name: debug
description: Pointer to the registered `engineering:debug` skill — invoke that for structured debugging with reproduce, isolate, diagnose, and fix phases.
---

# debug

This skill is a pointer. Use the registered `engineering:debug` skill for actual debugging work; it covers reproduction, root cause analysis, and structured fix workflows.

For guided debugging sessions invoke `engineering:debug`. For specialist dispatch on complex bugs, see `agents/debugger.md`.
